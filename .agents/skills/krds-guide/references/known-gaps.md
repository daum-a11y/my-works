# KRDS Known Gaps and Edge Notes

Use this file when official KRDS pages appear inconsistent or incomplete.

## 1. Style Range

The original research target was `style_01` through `style_07`, but the current KRDS site also exposes:

- `style_08`: 엘리베이션

The intro material references elevation, but the dedicated page sits outside the requested `01` to `07` range.

## 2. Component Summary Count

The component summary page shows `총 37건`, but the visible item list is larger.

Use the actual visible menu list and verified links, not the displayed count.

## 3. Basic Pattern Summary Count

The basic pattern summary page shows `총 11건`, but the visible list is `12` items because `모바일` is also present.

Again, trust the visible menu list over the count badge.

## 4. Component Numbering Gaps

Observed gaps in the current component URL scheme:

- `component_04_12.html` not visible on the summary page
- `component_06_05.html` not visible
- `component_06_06.html` not visible

Do not infer missing component names from numbering.

## 5. Help Panel Category Placement

`도움 패널` currently appears under the 입력 category on the summary page.

Use the official category when mapping links, but reason about its behavior as a help/support surface when designing.

## 6. Service Pattern Entry Pages

Current service pattern summary links resolve to:

- `service_01_01.html`
- `service_02_01.html`
- `service_03_01.html`
- `service_04_01.html`
- `service_05_01.html`

Treat these as the verified entry pages for each service pattern.

## 7. Preferred Source of Truth in This Repository

When you need a stable, pre-verified map, use:

- `/Volumes/workspace/workspace/my-works/.agents/skills/krds-guide/references/research/component-inventory.md`
- `/Volumes/workspace/workspace/my-works/.agents/skills/krds-guide/references/research/pattern-inventory.md`

Those files already capture the researched URLs and category mapping.
